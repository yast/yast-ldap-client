/**
 * File:
 *   ldap_conf.scr
 * Summary:
 *   SCR Agent for reading/writing /etc/openldap/ldap.conf using the ini-agent
 * Access:
 *   read/write
 * Authors:
 *   Petr Blahos <pblahos@suse.cz>
 * See:
 *   yast2-agent-ini
 *   libscr
 *   man ldap.conf
 * Example:
 *   Dir(.etc.openldap.ldap_conf)
 *   (["base", "host"])
 **
 *   Read(.etc.openldap.ldap_conf.host)
 *   ("10.10.0.149")
 **
 *   Write(.etc.openldap.ldap_conf.base, "dc=myodmain, dc=com")
 *   (true)
 *
 * $Id$
 *
 *    .etc.openldap.ldap_conf
 */
.etc.openldap.ldap_conf

`ag_ini(
  SCR::IniAgent(
    "/etc/openldap/ldap.conf",
    $[ "options" : [ "ignore_case", "global_values", "flat" ],
       "comments" : [ "^#.*", "^[ \t]*$", ],
       "params" : [
         $[ "match" : [ "^[ \t]*([a-zA-Z0-9_]+)[ \t]+(.*[^ \t]|)[ \t]*$",
			"%s\t%s",
		      ]
	 ]
       ]
    ]))
