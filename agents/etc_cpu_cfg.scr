/**
 * File:
 *   etc_cpu_cfg.scr
 * Summary:
 *   SCR Agent for reading/writing /etc/cpu.cfg
 * Access:
 *   read/write
 * Authors:
 *   Jiri Suchomel <jsuchome@suse.cz>
 * See:
 *   yast2-agent-ini
 *   man cpu
 * Example:
 *   Dir(.etc.cpu_cfg)
 *   (["ldap_host", "ldap_port", "bind_dn", "bind_pass", "base_dn"])
 **
 *   Read(.etc.cpu_cfg."ldap_host")
 *   ("10.10.0.149")
 **
 *   Write(.etc.cpu_cfg."base_dn", "dc=myodmain, dc=com")
 *   (true)
 *
 * $Id$
 *
 *    .etc.cpu_cfg
 */
.etc.cpu_cfg

`ag_ini(
  IniAgent(
    "/etc/cpu.cfg",
    $[ "options" : [ "ignore_case", "global_values", "flat" ],
       "comments" : [ "^#.*", "^[ \t]*$", ],
       "params" : [
		 $[ "match" : [ "^[ \t]*([^::]*[^ \t::])[ \t]*::[ \t]*(.*[^ \t]|)[ \t]*$" , "%s::%s"],],
       ]
	]
))
